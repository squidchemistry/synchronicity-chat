{
  "compilerOptions": {
    "target": "ESNext", // Use modern JavaScript features
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ], // Support for modern browsers and JS features
    "allowJs": true, // Allow JavaScript files to be compiled
    "skipLibCheck": true, // Skip type checking of declaration files
    "strict": true, // Enable strict type-checking options
    "noEmit": true, // Prevent output files (not needed for Next.js)
    "esModuleInterop": true, // Enable default imports for non-ES modules
    "module": "ESNext", // Use ESNext modules for optimal bundling
    "moduleResolution": "node", // Use Node's module resolution algorithm
    "resolveJsonModule": true // Allow importing JSON files as modules
    , // Allow importing JSON files
    "isolatedModules": true, // Ensures each file can be transpiled independently
    "jsx": "preserve", // Use React JSX Transform (React 17+)
    "outDir": "dist", // Output directory for compiled files (not needed in Next.js)
    "incremental": true, // Enable incremental compilation for faster builds
    "plugins": [
      {
        "name": "next" // Next.js plugin for TypeScript optimization
      }
    ],
    "paths": {
      "@/*": [
        "./src/*"
      ] // Allow absolute imports from 'src' directory
    },
    "baseUrl": "." // Set base URL for module resolution (optional)
  },
  "include": [
    "next-env.d.ts", // Automatically generated by Next.js
    "**/*.ts", // Include all TypeScript files
    "**/*.tsx", // Include all TSX files
    ".next/types/**/*.ts", // Include Next.js types
    "server/**/*.ts" // Include server-side code (optional)
  ],
  "exclude": [
    "node_modules",
    "dist"
  ] // Exclude the 'node_modules' and output folder
}
